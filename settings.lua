local settings = {}

settings.INSTRUCITONS_PER_FRAME = 10 -- Default 10 or 1
settings.FRAMES_PER_SECOND = 60 -- Default 60

settings.SCREEN_WIDTH = 64
settings.SCREEN_HEIGHT = 32
settings.SCREEN_SCALE = 15 -- Scale the screen by this factor

settings.DEBUG_MODE = true
settings.ROM_NAME = "6-keypad.ch8"

return settings
